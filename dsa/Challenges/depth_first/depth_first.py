from dsa.data_structures.graph.graph import Graph

def DFS(graph, value, discovered):
    discovered[value] = true
	print(value, end=’ ‘)
	for item in graph.adjList[v]
		if not discovered[item]:
			DFS(graph, item, discovered)



