from dsa.data_structures.graph.graph import Graph

def flight_available(graph, array):
    visited = [False] * self.vertices
	queue = []
	queue.append(graph)
	while queue:
		dequeue = queue.pop(0)
		if dequeue = array[i]
		    return true
	return False


